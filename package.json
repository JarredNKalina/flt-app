{
  "name": "ftlot-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon",
    "docker:dev": "npm run docker:down && docker-compose -f docker/development/docker-compose.dev.yml up --build --remove-orphans -d",
    "docker:down": "npm run docker:dev:down",
    "docker:dev:down": "docker-compose -f docker/development/docker-compose.dev.yml down",
    "db:seed": "ts-node prisma/seed.ts",
    "prisma:generate": "prisma generate",
    "build": "rm -rf dist && prisma generate && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.4",
    "nodemon": "^3.1.9",
    "prisma": "^6.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
